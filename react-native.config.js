module.exports = {
    project: {
      ios: {},
      android: {}, // grouped into "project"
    },
    assets: ["./assets/fonts"], // stays the same path where fonts save
  };

  //import './assets/fonts'